# QUICKSTART

このファイルは、`dev-rules` を自分のGitHubリポジトリへ導入するための最短手順です。

READMEは「dev-rulesとは何か」を説明する入口です。  
このQUICKSTARTは「実際に何を置いて、AIに何を頼むか」をまとめています。

---

## 目的

AIにコードを書かせる前に、リポジトリへ開発ルールを置きます。

これにより、ChatGPT / Claude / Codex などに対して、次のことを先に共有できます。

- 勝手に既存コードを壊さない
- 変更前に影響範囲を確認する
- 存在しないファイルや関数を前提にしない
- 現在の作業範囲から脱線しない
- 判断理由や未対応タスクをドキュメントへ残す

---

## まず作るファイル

自分のプロジェクトに、次のような `docs/` フォルダを用意します。

```text
your-project/
└─ docs/
   ├─ RULES.md
   ├─ DEVELOPMENT_PLAN.md
   ├─ DECISIONS.md
   ├─ BACKLOG.md
   └─ MODULE_INDEX.md
```

最初から完璧に埋めなくて大丈夫です。  
まずは空ファイルでもよいので、置き場所を決めることが大事です。

---

## Step 1: `docs/` フォルダを作る

ターミナルを使う場合は、プロジェクトのルートで次を実行します。

```bash
mkdir -p docs
```

Windows PowerShellの場合はこちらでもOKです。

```powershell
New-Item -ItemType Directory -Force -Path docs
```

GitHub上で作業する場合は、`docs/RULES.md` のようにファイル名を入力すれば、`docs` フォルダも一緒に作られます。

---

## Step 2: `RULES_TEMPLATE.md` を `docs/RULES.md` にコピーする

このリポジトリの `RULES_TEMPLATE.md` を、自分のプロジェクトの `docs/RULES.md` としてコピーします。

置き換える主な項目は次の通りです。

```text
{{プロジェクト名}} → 自分のプロジェクト名
{{日付}} → 作成日または更新日
{{記入}} → プロジェクト固有の内容
```

例：

```text
# SNS自動投稿アプリ - 開発ルール

最終更新: 2026-07-01
```

---

## Step 3: 管理用ファイルを作る

最低限、次の4つを用意します。

### `docs/DEVELOPMENT_PLAN.md`

現在の開発状況と、次にやることを書くファイルです。

最小例：

```markdown
# DEVELOPMENT_PLAN

## 現在の目的

月額定額AIとGitHubを使って、小さなアプリを安全に開発する。

## 現在のフェーズ

初期セットアップ

## 次にやること

1. READMEを整える
2. 最小機能を決める
3. 実装対象ファイルを確認する
```

### `docs/DECISIONS.md`

なぜその設計にしたのかを残すファイルです。

最小例：

```markdown
# DECISIONS

## D-001: 初期構成

- 日付: 2026-07-01
- 背景: AIに作業を依頼する前に、ルールと計画を固定したい
- 決定: docs/RULES.md と docs/DEVELOPMENT_PLAN.md を必須ファイルにする
- 理由: 会話が長くなっても、開発方針を引き継げるようにするため
```

### `docs/BACKLOG.md`

今はやらない改善案を置くファイルです。

最小例：

```markdown
# BACKLOG

| 日付 | 内容 | 発生経緯 | 優先度 | 状態 |
|---|---|---|---|---|
| 2026-07-01 | UI改善 | 初期実装中に思いついた | 低 | 未着手 |
```

### `docs/MODULE_INDEX.md`

主要ファイルや関数の場所をメモするファイルです。

最小例：

```markdown
# MODULE_INDEX

| ファイル | 役割 | 主な関数・クラス | 備考 |
|---|---|---|---|
| README.md | プロジェクト説明 | - | 最初に読む |
| src/main.py | アプリ本体 | main | 存在確認後に更新 |
```

---

## Step 4: AIに最初に渡す指示文

ChatGPT / Claude / Codex に作業を頼む前に、まず次の文を渡します。

```text
このリポジトリでは docs/RULES.md を開発ルールとして使います。

まず docs/RULES.md と docs/DEVELOPMENT_PLAN.md を読んでください。
コードを書く前に、以下を宣言してください。

1. 作業目的
2. 変更対象ファイル
3. 影響を受ける可能性があるファイル
4. 実装前の確認方法
5. テストまたは動作確認方法
6. ロールバック方法

確認が終わるまで、コード変更はしないでください。
```

ポイントは、**いきなりコードを書かせないこと**です。

AIには、まず「どこを触るつもりか」を宣言させます。

---

## Step 5: 最初に頼む小さい作業例

最初は、影響範囲が小さい作業から始めます。

おすすめの依頼例：

```text
README.mdの説明を初心者にも分かりやすくしてください。
変更前に、変更対象ファイル・影響範囲・確認方法を宣言してください。
```

```text
docs/DEVELOPMENT_PLAN.md を読み、次にやるべき作業を3つだけ提案してください。
まだコードは変更しないでください。
```

```text
現在のファイル構成を確認し、docs/MODULE_INDEX.md の初版を作ってください。
存在確認できたファイルだけを書いてください。
```

---

## Step 6: やってはいけない頼み方

次のような頼み方は危険です。

```text
このアプリをいい感じに直して
```

```text
全部リファクタリングして
```

```text
エラーが出てるから適当に直して
```

```text
必要そうなファイルを全部作って
```

理由は、作業範囲が広すぎるためです。  
AIが勝手にファイル構成を変えたり、不要な修正を入れたり、既存コードを壊す可能性が高くなります。

---

## Step 7: 安全な頼み方

安全な依頼は、次の形にします。

```text
目的：READMEの説明を初心者向けに直したい
対象：README.mdのみ
制約：他のファイルは変更しない
作業前：変更方針を3行で説明
作業後：変更点を箇条書きで報告
確認：Markdownとして崩れていないか確認
```

または、次のように短くても構いません。

```text
README.mdだけを対象に、初心者向けに説明を改善してください。
他のファイルは変更しないでください。
変更前に方針を出してください。
```

---

## Step 8: 作業が終わったら記録する

作業が終わったら、必要に応じて次のファイルを更新します。

- 進捗が変わった → `docs/DEVELOPMENT_PLAN.md`
- 設計判断をした → `docs/DECISIONS.md`
- 今はやらない案が出た → `docs/BACKLOG.md`
- ファイル構成が変わった → `docs/MODULE_INDEX.md`

AIとの会話は消えたり長くなったりします。  
重要なことは、会話ではなくリポジトリ側に残します。

---

## 最小セットで始める場合

まず試すだけなら、最小構成はこの2ファイルでOKです。

```text
docs/RULES.md
docs/DEVELOPMENT_PLAN.md
```

ただし、継続して開発するなら、早めに次の2つも追加するのがおすすめです。

```text
docs/DECISIONS.md
docs/BACKLOG.md
```

---

## 次に読むもの

- `README.md`：dev-rulesの全体像
- `RULES_TEMPLATE.md`：開発ルール本体
- `templates/HEARING_SHEET.md`：作りたいアプリの要件整理
- `examples/postcraft_rules.md`：実プロジェクトへの適用例

---

## まとめ

AIにコードを書かせる前に、まずルールを置きます。

```text
ルールを置く
↓
AIに読ませる
↓
変更範囲を宣言させる
↓
小さい作業から頼む
↓
判断と進捗を docs に残す
```

これだけで、AI開発の事故はかなり減らせます。
